CREATE TABLE students (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    studentID INT,
    firstName VARCHAR(50),
    lastName VARCHAR(50),
    phoneNumber VARCHAR(20),
    email VARCHAR(100),
    address VARCHAR(255)
);